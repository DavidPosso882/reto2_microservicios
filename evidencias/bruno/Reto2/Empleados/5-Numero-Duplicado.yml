info:
  name: 5-Numero-Duplicado
  type: http
  seq: 4

http:
  method: POST
  url: "{{empleados_url}}/empleados"
  body:
    type: json
    data: |-
      {
        "id": "E003",
        "nombre": "Ana",
        "apellido": "Lopez",
        "email": "ana@empresa.com",
        "numeroEmpleado": "EMP-2026-001",
        "cargo": "QA",
        "area": "Tecnologia",
        "departamentoId": "IT",
        "fechaIngreso": "2026-02-11"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Responde 400 Bad Request", function() {
          expect(res.getStatus()).to.equal(400);
        });
        test("Body status 400", function() {
          expect(res.getBody().status).to.equal(400);
        });
        test("Error numeroEmpleado duplicado", function() {
          expect(res.getBody().error).to.include("El numeroEmpleado 'EMP-2026-001' ya está registrado");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
