info:
  name: 3-Consulta-Existente
  type: http
  seq: 2

http:
  method: GET
  url: "{{empleados_url}}/empleados/E001"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Responde 200 OK", function() {
          expect(res.getStatus()).to.equal(200);
        });
        test("Devuelve el id E001", function() {
          expect(res.getBody().id).to.equal("E001");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
